name: Auto Build

on:
  push:
    branches:
      - dev
      - main
      - master
  workflow_dispatch:

permissions:
  contents: write
env:
  TAG_NAME: autobuild
  TAG_CHANNEL: AutoBuild
  CARGO_INCREMENTAL: 0
  RUST_BACKTRACE: short
  HUSKY: 0
  CARGO_NET_RETRY: '10'
  CARGO_HTTP_TIMEOUT: '120'
  CARGO_HTTP_MULTIPLEXING: 'false'
  PNPM_CONFIG_FETCH_RETRIES: '5'
  PNPM_CONFIG_FETCH_RETRY_FACTOR: '2'
  PNPM_CONFIG_FETCH_RETRY_MINTIMEOUT: '20000'
  PNPM_CONFIG_FETCH_RETRY_MAXTIMEOUT: '120000'
concurrency:
  group: '${{ github.workflow }} - ${{ github.ref }}'
  cancel-in-progress: true

jobs:
  update_tag:
    name: Update tag
    runs-on: ubuntu-latest
    timeout-minutes: 20
    outputs:
      should_build: ${{ steps.check_latest.outputs.should_build }}
      release_id: ${{ steps.upload_release.outputs.id }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7
        timeout-minutes: 10

      - name: Check latest branch commit
        id: check_latest
        shell: bash
        timeout-minutes: 10
        run: |
          latest_sha=$(bash .github/scripts/retry-command --timeout-seconds 120 -- \
            git ls-remote origin "refs/heads/$GITHUB_REF_NAME" | cut -f1)
          if [ "$GITHUB_SHA" = "$latest_sha" ]; then
            echo "should_build=true" >> "$GITHUB_OUTPUT"
          else
            echo "should_build=false" >> "$GITHUB_OUTPUT"
            echo "Skip outdated commit $GITHUB_SHA; latest is $latest_sha"
          fi

      - name: Set build time
        if: steps.check_latest.outputs.should_build == 'true'
        run: |
          echo "BUILDTIME=$(TZ=Asia/Shanghai date)" >> $GITHUB_ENV
        shell: bash

      - name: Create release notes
        if: steps.check_latest.outputs.should_build == 'true'
        run: |
          cat > release.txt << EOF
          此预发布始终对应仓库最新一次成功的自动构建，旧资产会在新构建开始时清理。

          构建提交：${{ github.sha }}
          构建时间：${{ env.BUILDTIME }}
          EOF

      - name: Move autobuild tag
        if: steps.check_latest.outputs.should_build == 'true'
        timeout-minutes: 10
        run: |
          git tag --force "$TAG_NAME" "$GITHUB_SHA"
          bash .github/scripts/retry-command --timeout-seconds 120 -- \
            git push origin "refs/tags/$TAG_NAME" --force

      - name: Upload Release
        if: steps.check_latest.outputs.should_build == 'true'
        id: upload_release
        uses: softprops/action-gh-release@v3
        timeout-minutes: 10
        with:
          tag_name: ${{ env.TAG_NAME }}
          target_commitish: ${{ github.sha }}
          name: 'Clash Verge Rev ${{ env.TAG_CHANNEL }}'
          body_path: release.txt
          prerelease: true
          token: ${{ secrets.GITHUB_TOKEN }}
          generate_release_notes: false

      - name: Clean old release assets
        if: steps.check_latest.outputs.should_build == 'true'
        timeout-minutes: 15
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          assets=$(bash .github/scripts/retry-command --timeout-seconds 120 -- \
            gh release view "$TAG_NAME" --json assets --jq '.assets[].name')
          if [ -z "$assets" ]; then
            exit 0
          fi
          while IFS= read -r asset; do
            bash .github/scripts/retry-command --timeout-seconds 120 -- \
              gh release delete-asset "$TAG_NAME" "$asset" --yes
          done <<< "$assets"

  autobuild-x86-windows-macos-linux:
    name: Autobuild x86 Windows, MacOS and Linux
    needs: update_tag
    if: needs.update_tag.outputs.should_build == 'true'
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            target: x86_64-pc-windows-msvc
          - os: windows-latest
            target: aarch64-pc-windows-msvc
          - os: macos-latest
            target: aarch64-apple-darwin
          - os: macos-latest
            target: x86_64-apple-darwin
          - os: ubuntu-22.04
            target: x86_64-unknown-linux-gnu
    runs-on: ${{ matrix.os }}
    timeout-minutes: 120
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v7
        timeout-minutes: 10

      - name: Install Rust Stable
        uses: dtolnay/rust-toolchain@master
        timeout-minutes: 15
        with:
          toolchain: '1.95.0'
          targets: ${{ matrix.target }}

      - name: Rust Cache
        uses: Swatinem/rust-cache@v2
        timeout-minutes: 15
        with:
          save-if: ${{ github.ref == 'refs/heads/dev' }}
          prefix-key: 'v1-rust'
          key: 'rust-shared-stable-${{ matrix.os }}-${{ matrix.target }}'
          workspaces: |
            . -> target
          cache-all-crates: true
          cache-workspace-crates: true

      - name: Install dependencies (ubuntu only)
        if: matrix.os == 'ubuntu-22.04'
        timeout-minutes: 20
        run: |
          bash .github/scripts/retry-command --timeout-seconds 300 -- \
            sudo apt-get update
          bash .github/scripts/retry-command --timeout-seconds 600 -- \
            sudo apt-get install -y libxslt1.1 libwebkit2gtk-4.1-dev \
              libayatana-appindicator3-dev librsvg2-dev patchelf

      - uses: pnpm/action-setup@v6.0.10
        name: Install pnpm
        timeout-minutes: 10
        with:
          run_install: false

      - name: Install Node
        uses: actions/setup-node@v7
        timeout-minutes: 15
        with:
          node-version: '24.19.0'
          cache: 'pnpm'

      - name: Pnpm install and check
        timeout-minutes: 30
        shell: bash
        run: |
          bash .github/scripts/retry-command --timeout-seconds 600 -- \
            pnpm install --frozen-lockfile
          bash .github/scripts/retry-command --timeout-seconds 900 -- \
            pnpm run prebuild ${{ matrix.target }}

      - name: Release ${{ env.TAG_CHANNEL }} Version
        run: pnpm release-version autobuild-latest

      - name: Tauri build for Windows-macOS-Linux
        uses: tauri-apps/tauri-action@v1
        timeout-minutes: 90
        env:
          NODE_OPTIONS: '--max_old_space_size=4096'
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          releaseId: ${{ needs.update_tag.outputs.release_id }}
          tagName: ${{ env.TAG_NAME }}
          releaseName: 'Clash Verge Rev ${{ env.TAG_CHANNEL }}'
          releaseBody: 'More new features are now supported.'
          releaseDraft: false
          prerelease: true
          tauriScript: pnpm
          args: --target ${{ matrix.target }}

  autobuild-arm-linux:
    name: Autobuild ARM Linux
    needs: update_tag
    if: needs.update_tag.outputs.should_build == 'true'
    strategy:
      fail-fast: false
      matrix:
        include:
          # It should be ubuntu-22.04 to match the cross-compilation environment
          # ortherwise it is hard to resolve the dependencies
          - os: ubuntu-22.04
            target: aarch64-unknown-linux-gnu
            arch: arm64
          - os: ubuntu-22.04
            target: armv7-unknown-linux-gnueabihf
            arch: armhf
    runs-on: ${{ matrix.os }}
    timeout-minutes: 180
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v7
        timeout-minutes: 10

      - name: Install Rust Stable
        uses: dtolnay/rust-toolchain@master
        timeout-minutes: 15
        with:
          toolchain: '1.95.0'
          targets: ${{ matrix.target }}

      - name: Rust Cache
        uses: Swatinem/rust-cache@v2
        timeout-minutes: 15
        with:
          save-if: ${{ github.ref == 'refs/heads/dev' }}
          prefix-key: 'v1-rust'
          key: 'rust-shared-stable-${{ matrix.os }}-${{ matrix.target }}'
          workspaces: |
            . -> target
          cache-all-crates: true
          cache-workspace-crates: true

      - name: Install pnpm
        uses: pnpm/action-setup@v6.0.10
        timeout-minutes: 10
        with:
          run_install: false

      - name: Install Node
        uses: actions/setup-node@v7
        timeout-minutes: 15
        with:
          node-version: '24.19.0'
          cache: 'pnpm'

      - name: Pnpm install and check
        timeout-minutes: 30
        run: |
          bash .github/scripts/retry-command --timeout-seconds 600 -- \
            pnpm install --frozen-lockfile
          bash .github/scripts/retry-command --timeout-seconds 900 -- \
            pnpm run prebuild ${{ matrix.target }}

      - name: Release ${{ env.TAG_CHANNEL }} Version
        run: pnpm release-version autobuild-latest

      - name: 'Setup for linux'
        timeout-minutes: 30
        run: |-
          sudo ls -lR /etc/apt/

          cat > /tmp/sources.list << EOF
          deb [arch=amd64,i386] http://archive.ubuntu.com/ubuntu jammy main multiverse universe restricted
          deb [arch=amd64,i386] http://archive.ubuntu.com/ubuntu jammy-security main multiverse universe restricted
          deb [arch=amd64,i386] http://archive.ubuntu.com/ubuntu jammy-updates main multiverse universe restricted
          deb [arch=amd64,i386] http://archive.ubuntu.com/ubuntu jammy-backports main multiverse universe restricted

          deb [arch=armhf,arm64] http://ports.ubuntu.com/ubuntu-ports jammy main multiverse universe restricted
          deb [arch=armhf,arm64] http://ports.ubuntu.com/ubuntu-ports jammy-security main multiverse universe restricted
          deb [arch=armhf,arm64] http://ports.ubuntu.com/ubuntu-ports jammy-updates main multiverse universe restricted
          deb [arch=armhf,arm64] http://ports.ubuntu.com/ubuntu-ports jammy-backports main multiverse universe restricted
          EOF

          sudo mv /etc/apt/sources.list /etc/apt/sources.list.default
          sudo mv /tmp/sources.list /etc/apt/sources.list

          sudo dpkg --add-architecture ${{ matrix.arch }}
          bash .github/scripts/retry-command --timeout-seconds 300 -- \
            sudo apt update

          bash .github/scripts/retry-command --timeout-seconds 900 -- \
            sudo apt install -y \
              libxslt1.1:${{ matrix.arch }} \
              libwebkit2gtk-4.1-dev:${{ matrix.arch }} \
              libayatana-appindicator3-dev:${{ matrix.arch }} \
              libssl-dev:${{ matrix.arch }} \
              patchelf:${{ matrix.arch }} \
              librsvg2-dev:${{ matrix.arch }}

      - name: Install aarch64 tools
        if: matrix.target == 'aarch64-unknown-linux-gnu'
        timeout-minutes: 20
        run: |
          bash .github/scripts/retry-command --timeout-seconds 600 -- \
            sudo apt install -y \
              gcc-aarch64-linux-gnu \
              g++-aarch64-linux-gnu

      - name: Install armv7 tools
        if: matrix.target == 'armv7-unknown-linux-gnueabihf'
        timeout-minutes: 20
        run: |
          bash .github/scripts/retry-command --timeout-seconds 600 -- \
            sudo apt install -y \
              gcc-arm-linux-gnueabihf \
              g++-arm-linux-gnueabihf

      - name: Tauri Build for Linux
        timeout-minutes: 165
        run: |
          export PKG_CONFIG_ALLOW_CROSS=1
          if [ "${{ matrix.target }}" == "aarch64-unknown-linux-gnu" ]; then
            export PKG_CONFIG_PATH=/usr/lib/aarch64-linux-gnu/pkgconfig/:$PKG_CONFIG_PATH
            export PKG_CONFIG_SYSROOT_DIR=/usr/aarch64-linux-gnu/
          elif [ "${{ matrix.target }}" == "armv7-unknown-linux-gnueabihf" ]; then
            export PKG_CONFIG_PATH=/usr/lib/arm-linux-gnueabihf/pkgconfig/:$PKG_CONFIG_PATH
            export PKG_CONFIG_SYSROOT_DIR=/usr/arm-linux-gnueabihf/
          fi
          bash .github/scripts/retry-command --timeout-seconds 3300 -- \
            pnpm build --target ${{ matrix.target }}
        env:
          NODE_OPTIONS: '--max_old_space_size=4096'

      - name: Upload Release
        timeout-minutes: 30
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          assets=(
            target/${{ matrix.target }}/release/bundle/deb/*.deb
            target/${{ matrix.target }}/release/bundle/rpm/*.rpm
          )
          bash .github/scripts/retry-command --timeout-seconds 600 -- \
            gh release upload "$TAG_NAME" "${assets[@]}" --clobber

  rename-release-assets:
    name: Rename release assets
    needs: [update_tag, autobuild-x86-windows-macos-linux, autobuild-arm-linux]
    if: needs.update_tag.outputs.should_build == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v7
        timeout-minutes: 10

      - name: Rename uploaded assets
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: bash .github/scripts/rename-release-assets "$TAG_NAME"
